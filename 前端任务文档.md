# 知识库管理平台 —— 前端任务文档

> 版本：v1.0 ｜ 更新日期：2026-08-28
> 上游依据：《需求文档.md》2.9 节、《backend/md/前端开发手册.md》、《backend/md/后端接口文档.md》
> 开发原则：**从简、以功能实现为主**（纯 HTML + CSS + 原生 JS，无框架、无构建工具）

---

## 1. 任务总览

| 阶段 | 任务 | 内容 | 优先级 |
| --- | --- | --- | --- |
| F1 | 公共基础设施 | 目录骨架、`common.js` 请求封装、基础样式 | P0 |
| F2 | 登录页 | 登录 + token 存储 + 跳转 | P0 |
| F3 | 首页导航 | 顶部导航 + 动态菜单 + 权限控制 | P0 |
| F4 | 组织架构 | 用户/角色/部门管理 | P1 |
| F5 | 知识单元 | 列表 + 编辑 + 权限配置 + 导入 | P1 |
| F6 | AI 对话 | SSE 流式问答 + 会话历史 | P0 |
| F7 | 数据看板 | 指标 + 排行 + 趋势图表 | P2 |
| F8 | 知识沉淀 | FAQ 审核 + 知识缺口 | P2 |

> 优先级说明：P0 为核心链路（登录→对话），P1 为管理功能，P2 为展示类，可后置。

---

## 2. 任务拆解

### F1 公共基础设施

| 编号 | 任务 | 内容 | 交付物 |
| --- | --- | --- | --- |
| T1.1 | 目录骨架 | 创建 `frontend/` 目录、`js/`、`css/` 子目录 | 目录结构 |
| T1.2 | common.js | `request` 封装（自动注入 token、401 跳转）、`hasPermission`、`getToken` 等 | 公共工具 |
| T1.3 | common.css | 基础样式（布局、表格、按钮、表单、卡片） | 公共样式 |

**验收**：`common.js` 可复用，请求自动带 token，401 自动跳登录。

### F2 登录页

| 编号 | 任务 | 内容 | 交付物 |
| --- | --- | --- | --- |
| T2.1 | login.html | 用户名/密码表单 + 登录按钮 | 登录页 |
| T2.2 | 登录逻辑 | 调 `POST /api/auth/login`，存 `access_token`/`user_info`/`permissions` 到 localStorage，跳转首页 | 登录功能 |

**验收**：登录成功跳首页，失败提示错误；token 正确保存。

### F3 首页导航

| 编号 | 任务 | 内容 | 交付物 |
| --- | --- | --- | --- |
| T3.1 | index.html | 顶部导航栏 + 功能入口 | 首页 |
| T3.2 | 登录态守卫 | 页面加载检查 token，无 token 跳登录页 | 守卫逻辑 |
| T3.3 | 动态菜单 | 根据 `permissions` 控制菜单项显隐 | 菜单控制 |

**验收**：无 token 自动跳登录；不同角色看到的菜单不同。

### F4 组织架构

| 编号 | 任务 | 内容 | 交付物 |
| --- | --- | --- | --- |
| T4.1 | 部门管理 | 部门树展示 + 新增/编辑/删除 | 部门模块 |
| T4.2 | 用户管理 | 用户列表 + 新增/编辑/重置密码/启停用 | 用户模块 |
| T4.3 | 角色管理 | 角色列表 + 权限分配 | 角色模块 |

**验收**：部门树正确渲染；用户 CRUD 可用；权限分配可提交。

> 从简策略：三个子模块可合并在一个 `org.html` 页面，用 Tab 切换。

### F5 知识单元

| 编号 | 任务 | 内容 | 交付物 |
| --- | --- | --- | --- |
| T5.1 | 单元列表 | 分页列表 + 搜索/筛选 + 删除 | 列表页 |
| T5.2 | 单元编辑 | 编辑表单（标题/正文/标签/状态） | 编辑功能 |
| T5.3 | 数据权限配置 | 四类权限勾选弹窗（全局/部门/角色/个人） | 权限配置 |
| T5.4 | 知识导入 | 文件上传 + 进度轮询 | 导入功能 |

**验收**：列表分页正确；编辑/删除可用；权限配置可提交；导入能显示进度。

### F6 AI 对话

| 编号 | 任务 | 内容 | 交付物 |
| --- | --- | --- | --- |
| T6.1 | chat.html | 对话界面（消息列表 + 输入框 + 发送按钮） | 对话页 |
| T6.2 | SSE 流式 | `POST /api/ai/chat/stream` 流式接收 + 逐段渲染 | 流式问答 |
| T6.3 | 多轮对话 | 保存 `session_id`，回传实现多轮 | 多轮对话 |
| T6.4 | 会话历史 | 会话列表 + 历史消息加载 | 历史功能 |

**验收**：提问后逐段流式显示回答；多轮对话上下文连贯；能查看历史会话。

> SSE 示例见接口文档附录 C（原生 fetch + ReadableStream，可直接复用）。

### F7 数据看板

| 编号 | 任务 | 内容 | 交付物 |
| --- | --- | --- | --- |
| T7.1 | 指标卡片 | 调 `/api/dashboard/metrics` 展示 5 个指标 | 指标卡 |
| T7.2 | 排行榜 | 常见问题 TOP + 知识单元 TOP | 榜单 |
| T7.3 | 趋势图 | ECharts 渲染 Token/耗时趋势（日/周切换） | 图表 |

**验收**：指标正确展示；榜单数据正确；趋势图可切换日/周。

### F8 知识沉淀

| 编号 | 任务 | 内容 | 交付物 |
| --- | --- | --- | --- |
| T8.1 | FAQ 审核 | 待审核列表 + 通过/驳回操作 | FAQ 审核 |
| T8.2 | 已发布 FAQ | 已发布列表展示 | FAQ 列表 |
| T8.3 | 知识缺口 | 缺口列表 + 一键建档/关联 | 缺口管理 |

**验收**：FAQ 可审核（通过/驳回）；缺口可处理（建档/关联）。

---

## 3. 依赖关系

```
F1（公共基础）──> F2（登录）──> F3（首页导航）
                                  │
                    ┌─────────────┼─────────────┐
                    ▼             ▼             ▼
                 F4（组织）    F5（知识）     F6（对话，P0）
                                                  │
                                    F7（看板）──F8（沉淀）
```

- F6（AI 对话）为核心链路，可与 F4/F5 并行，优先实现
- F7/F8 依赖数据积累，可最后实现

---

## 4. 排期建议（从简）

| 阶段 | 建议工期 | 说明 |
| --- | --- | --- |
| F1 + F2 + F3 | 1 天 | 基础设施 + 登录 + 导航 |
| F6（AI 对话） | 1 天 | 核心功能优先 |
| F4（组织） | 1 天 | 表格/树/表单 |
| F5（知识） | 1 天 | 列表 + 权限 + 导入 |
| F7 + F8 | 1 天 | 图表 + 审核 |

> 总计约 5 个工作日，单页面从简实现。

---

## 5. 从简开发原则

1. **单文件页面**：每个模块一个 HTML 文件，JS/CSS 内联或抽公共文件，不拆组件。
2. **功能优先**：先保证核心流程跑通（登录→对话→管理），视觉美化后置。
3. **复用封装**：请求、权限、token 统一放 `common.js`，避免重复代码。
4. **轻量第三方**：图表用 ECharts CDN、Markdown 用 marked.js CDN，其余原生实现。
5. **降级容错**：接口报错统一 toast 提示，不阻塞页面。
6. **不做过度交互**：不做拖拽、动画、复杂表单校验，能用原生控件就用原生。

---

## 6. 关键注意事项

1. **导入是异步的**：上传后必须轮询 `/import/{task_id}`，不要期望同步返回。
2. **文件格式限制**：仅 TXT、Markdown，前端限制文件选择类型。
3. **部分接口无统一外壳**：登录、列表类接口直接返回对象，需按接口文档区分。
4. **数据权限配置是覆盖式**：一次性传完整列表。
5. **部门删除有前置校验**：有子部门/成员时报 400，需友好提示。
6. **多轮对话依赖 session_id**：每次回传上次的 session_id。
7. **FAQ 审核通过必须提供答案**：approve 时答案为空会报 40010。
