# 虚拟环境
.venv/
venv/
env/

# Python 缓存
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# 环境变量（不要打进镜像）
.env
.env.*

# 文档目录（构建时不需要）
md/

# 部署目录：只保留 docker-entrypoint.sh（Dockerfile 需要），排除其余
deploy/prod/
deploy/docker-compose.yml
deploy/.env.docker
deploy/.env.docker.example
deploy/README.md

# 其他
.git/
.gitignore
*.log
