# 后端详细需求说明书（知识库管理平台）

> 本文档由 PRD 2.9「知识库管理平台」拆解而来，聚焦领域模块、数据模型、业务规则、安全与非功能需求。

---

## 一、系统架构与模块划分

### 1.1 领域模块

| 模块 | 职责 | 对应 PRD 后端模块 |
| --- | --- | --- |
| 认证鉴权模块 | 用户认证、JWT 签发/校验、RBAC 操作权限拦截 | 认证鉴权模块 |
| 组织架构服务 | 用户/角色/部门树维护、映射关系 | 组织架构服务 |
| 知识单元管理服务 | 文档上传、格式解析、文本切片、CRUD、向量化同步 | 知识单元管理服务 |
| 数据权限引擎 | 计算用户对知识单元集合的访问权限，输出允许/拒绝列表 | 数据权限引擎 |
| AI 鉴权检索服务 | 会话管理、混合召回、权限过滤、Prompt 组装、流式回答 | AI 鉴权检索服务 |
| 数据看板统计服务 | 异步记录访问日志、聚合指标与排行榜 | 数据看板统计服务 |
| 知识沉淀挖掘服务 | 高频问题挖掘、生成 FAQ 推荐、识别知识缺口 | 知识沉淀挖掘服务 |
| FAQ 缓存服务 | 已发布 FAQ 语义匹配与快速应答缓存 | FAQ 缓存服务 |

### 1.2 技术组件

- 认证：JWT（Access Token + Refresh Token [待确认]）。
- 向量检索：向量数据库（如 Milvus/pgvector）存储知识单元 embedding。
- 消息队列：访问日志异步写入（RabbitMQ/Kafka [待确认]）。
- 定时任务：FAQ 挖掘与知识缺口聚合（[待确认]：Spring @Scheduled / Celery Beat）。
- 缓存：Redis（FAQ 缓存、Token 黑名单、限流计数）。

---

## 二、数据模型设计（DDL/表结构）

> 字段沿用 PRD 2.9.7 定义，补齐类型/约束/默认值/索引。

### 2.1 users（用户表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK AUTO_INCREMENT | 是 | - | 主键 |
| username | VARCHAR(32) UNIQUE | 是 | - | 登录名 |
| password_hash | VARCHAR(255) | 是 | - | 加密密码（bcrypt） |
| display_name | VARCHAR(50) | 是 | - | 显示名 |
| department_id | BIGINT | 否 | NULL | 所属部门 FK→departments.id |
| status | TINYINT | 是 | 1 | 1启用 0停用 |
| created_at / updated_at | DATETIME | 是 | CURRENT_TIMESTAMP | - |

索引：`uk_username(username)`、`idx_department(department_id)`。

### 2.2 departments（部门表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| parent_id | BIGINT | 否 | NULL | 父部门，NULL 为顶级 |
| name | VARCHAR(100) | 是 | - | 部门名 |
| leader_id | BIGINT | 否 | NULL | 负责人 FK→users.id |
| sort_order | INT | 是 | 0 | 排序 |
| created_at / updated_at | DATETIME | 是 | - | - |

索引：`idx_parent(parent_id)`。

### 2.3 roles（角色表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| role_name | VARCHAR(50) | 是 | - | 角色名 |
| role_code | VARCHAR(50) UNIQUE | 是 | - | 角色编码 |
| description | VARCHAR(255) | 否 | NULL | 描述 |
| created_at / updated_at | DATETIME | 是 | - | - |

### 2.4 user_roles（用户-角色关联表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| user_id | BIGINT | 是 | - | FK→users.id |
| role_id | BIGINT | 是 | - | FK→roles.id |
| created_at | DATETIME | 是 | - | - |

索引：`uk_user_role(user_id, role_id)`、`idx_role(role_id)`。

### 2.5 role_permissions（角色权限表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| role_id | BIGINT | 是 | - | FK→roles.id |
| permission_code | VARCHAR(100) | 是 | - | 权限码（菜单/操作） |
| permission_type | VARCHAR(20) | 是 | - | `menu` / `action` |
| created_at | DATETIME | 是 | - | - |

索引：`uk_role_perm(role_id, permission_code)`。

> 操作权限码约定：`knowledge:create`、`knowledge:update`、`knowledge:delete`、`knowledge:query`、`ai:access`。

### 2.6 knowledge_units（知识单元表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| unit_code | VARCHAR(64) UNIQUE | 是 | - | 单元编号 |
| title | VARCHAR(200) | 是 | - | 标题 |
| content | LONGTEXT | 是 | - | 正文（切片后正文） |
| summary | TEXT | 否 | NULL | 摘要 |
| category | VARCHAR(50) | 否 | NULL | 分类 |
| source_file_name | VARCHAR(255) | 否 | NULL | 源文件名 |
| file_type | VARCHAR(20) | 否 | NULL | PDF/MD/DOCX/TXT |
| file_size | BIGINT | 否 | NULL | 字节 |
| status | TINYINT | 是 | 1 | 1草稿 2已发布 3已删除 [待确认] |
| creator_id | BIGINT | 是 | - | FK→users.id |
| created_at / updated_at | DATETIME | 是 | - | - |

索引：`uk_unit_code`、`idx_category_status(category,status)`、`idx_creator(creator_id)`、`idx_updated(updated_at)`。

### 2.7 unit_permissions（数据权限表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| unit_id | BIGINT | 是 | - | FK→knowledge_units.id |
| target_type | VARCHAR(20) | 是 | - | `global`/`department`/`role`/`user` |
| target_id | BIGINT | 否 | NULL | 实体 ID（global 时为 NULL） |
| created_at | DATETIME | 是 | - | - |

索引：`idx_unit(unit_id)`、`idx_target(target_type, target_id)`。

### 2.8 qa_access_logs（问答访问日志表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| session_id | VARCHAR(64) | 是 | - | 会话 ID |
| user_id | BIGINT | 是 | - | FK→users.id |
| question | TEXT | 是 | - | 提问 |
| answer | LONGTEXT | 否 | NULL | 回答 |
| recalled_unit_ids_json | JSON | 否 | NULL | 召回单元 ID 列表 |
| authorized_unit_ids_json | JSON | 否 | NULL | 授权单元 ID 列表 |
| unauthorized_unit_ids_json | JSON | 否 | NULL | 未授权单元 ID 列表 |
| prompt_tokens / completion_tokens / total_tokens | INT | 是 | 0 | Token 计数 |
| response_time_ms | INT | 是 | - | 响应耗时 |
| created_at | DATETIME | 是 | - | - |

索引：`idx_user_time(user_id, created_at)`、`idx_session(session_id)`、`idx_created(created_at)`。

### 2.9 faqs（FAQ 表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| question | VARCHAR(500) | 是 | - | 问题 |
| answer | LONGTEXT | 是 | - | 答案 |
| category | VARCHAR(50) | 否 | NULL | 分类 |
| related_unit_id | BIGINT | 否 | NULL | 关联知识单元 |
| source_type | VARCHAR(20) | 是 | `manual` | `manual`/`auto_mined` |
| status | VARCHAR(20) | 是 | `pending_review` | `pending_review`/`published`/`rejected` |
| hit_count | INT | 是 | 0 | 命中次数 |
| reviewer_id | BIGINT | 否 | NULL | 审核人 |
| reviewed_at | DATETIME | 否 | NULL | 审核时间 |
| created_at / updated_at | DATETIME | 是 | - | - |

索引：`idx_status(status)`、`idx_related(related_unit_id)`。

### 2.10 knowledge_gaps（知识缺口表）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT PK | 是 | - | 主键 |
| question_pattern | VARCHAR(500) | 是 | - | 问题模式/聚类代表 |
| sample_questions_json | JSON | 否 | NULL | 样例问题列表 |
| ask_count | INT | 是 | 0 | 提问频次 |
| last_asked_at | DATETIME | 否 | NULL | 最近提问时间 |
| status | VARCHAR(20) | 是 | `unresolved` | `unresolved`/`resolved`/`ignored` |
| resolved_unit_id | BIGINT | 否 | NULL | 补全后关联单元 |
| created_at / updated_at | DATETIME | 是 | - | - |

索引：`idx_status(status)`、`idx_asked(last_asked_at)`。

---

## 三、业务规则与安全策略

### 3.1 权限校验逻辑

- **RBAC 操作权限**：登录签发 JWT，附带用户角色 → 每次请求经权限拦截器，校验角色 `role_permissions` 中是否含对应操作码。
- **数据权限（四维 OR 逻辑）**：用户对知识单元可访问当且仅当满足以下任一条件：
  1. 单元配置了 `global` 权限；
  2. 单元配置的 `department` 命中用户所属部门（含上级部门 [待确认]）；
  3. 单元配置的 `role` 命中用户拥有的任一角色；
  4. 单元配置的 `user` 命中用户本人。
- **默认无权限**：知识单元未配置任何权限时，任何人（除知识管理员在管理界面）不可通过 AI 检索访问。

### 3.2 敏感数据脱敏

- 密码仅存 `bcrypt` 哈希，任何接口不回传 `password_hash`。
- 日志中不记录完整密码；`qa_access_logs` 提问/回答内容按需脱敏（[待确认]：是否对个人信息脱敏）。
- 接口响应统一过滤 `password_hash` 等敏感字段。

### 3.3 幂等性处理

- 知识单元批量删除、权限批量配置：使用幂等键或按 `unit_id` 唯一约束去重，重复提交不产生副作用。
- 上传导入：以文件 MD5 去重，重复导入返回已存在提示 [待确认]。
- FAQ 审核：同一 FAQ 重复审核时状态机校验（仅 `pending_review` 可流转）。

### 3.4 限流策略

| 接口 | 限流 | 说明 |
| --- | --- | --- |
| `/api/auth/login` | 5 次/分钟/IP | 防暴力破解 |
| `/api/ai/chat/stream` | 10 次/分钟/用户 | 防 Token 滥用 |
| 上传导入 | 10 次/小时/用户 | 防恶意大文件 |
| 其他 | 100 次/分钟/用户 | 通用阈值 [待确认] |

实现：Redis 计数器 + 令牌桶。

### 3.5 并发与事务

- 数据权限配置：批量写入 `unit_permissions` 使用事务，失败整体回滚。
- 看板统计：日志写入走消息队列异步，不阻塞问答主链路。
- 事务隔离级别：默认 READ COMMITTED；权限配置、审核发布建议 REPEATABLE READ [待确认]。
- 乐观锁：知识单元编辑使用 `updated_at` 版本控制，防并发覆盖 [待确认]。

### 3.6 其他安全

- JWT 过期时间：Access 30min + Refresh 7d [待确认]。
- Token 注销：登出后 Token 加入 Redis 黑名单。
- 上传安全：文件类型白名单 + MIME 校验 + 大小限制，防路径穿越与恶意文件。
- SQL 注入/XSS：参数化查询；回答内容 Markdown 渲染前做 XSS 清洗（后端存储、前端渲染双层防护）。

---

## 四、非功能性需求（补充隐含需求）

| 类别 | 要求 |
| --- | --- |
| 性能 | 列表查询 P95 < 200ms；权限校验接口 P95 < 50ms；AI 首字响应 < 2s [待确认] |
| QPS | 鉴权接口支撑 ≥ 500 QPS；AI 问答 ≥ 50 QPS [待确认] |
| 可用性 | 服务 99.9%；AI 流式连接断线可重连 |
| 可扩展 | 知识单元、日志支持水平分库分表预留 [待确认] |
| 可观测 | 全链路日志、接口耗时埋点、错误告警 |
| 数据安全 | 定期备份，日志保留 90 天 [待确认] |
| 定时任务 | FAQ 挖掘与缺口聚合可配置调度周期（默认每日凌晨） |

---

## 五、[待确认] 事项汇总（后端）

1. 向量数据库选型（Milvus / pgvector）。
2. 消息队列选型（RabbitMQ / Kafka）。
3. 定时任务框架（Spring Scheduled / Celery Beat）。
4. JWT 是否双 Token（Access + Refresh）。
5. 部门数据权限是否含上级部门（建议：含上级）。
6. 知识单元 `status` 枚举值范围。
7. 上传 MD5 去重策略。
8. 限流具体阈值。
9. 事务隔离级别与乐观锁方案。
10. 性能指标（QPS/P95）目标值。
11. 日志保留周期与脱敏范围。
12. 全文检索引擎（Elasticsearch）是否引入 [待确认，若知识量较大建议引入]。
