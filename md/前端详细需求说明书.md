# 前端详细需求说明书（知识库管理平台）

> 本文档由 PRD 2.9「知识库管理平台」拆解而来，聚焦页面布局、交互、状态管理、路由与兼容性。
> 数据结构的字段定义见《后端详细需求说明书》，接口契约见《前后端接口契约（API蓝图）》。

---

## 一、页面路由与菜单结构

### 1.1 路由表

| 路由路径 | 页面名称 | 所属菜单 | 可访问角色 | 说明 |
| --- | --- | --- | --- | --- |
| `/login` | 登录页 | 无（公开） | 匿名 | 未登录唯一入口 |
| `/profile` | 个人中心页 | 个人中心 | 所有已登录角色 | 展示身份/部门/角色 |
| `/org/users` | 用户管理 | 组织架构与权限管理 | 系统管理员 | 用户 CRUD、重置密码、启停用 |
| `/org/roles` | 角色管理 | 组织架构与权限管理 | 系统管理员 | 角色维护、权限树分配 |
| `/org/departments` | 部门管理 | 组织架构与权限管理 | 系统管理员 | 部门树维护 |
| `/knowledge/import` | 知识导入中心 | 知识维护与导入 | 知识管理员 | 单/批量上传、进度展示 |
| `/knowledge/units` | 知识单元列表 | 知识维护与导入 | 知识管理员 | 搜索筛选、CRUD |
| `/knowledge/units/:id` | 知识单元编辑/详情 | 知识维护与导入 | 知识管理员 | 编辑内容与数据权限 |
| `/chat` | AI 对话鉴权工作台 | AI 对话 | 所有已登录角色 | 流式问答、引用与权限提示 |
| `/dashboard` | 数据看板页 | 数据看板 | 系统管理员 | 指标、榜单、趋势图 |
| `/settlement/faqs` | FAQ 推荐与审核 | 知识沉淀管理 | 知识管理员 | 推荐列表、审核流转 |
| `/settlement/faqs/published` | 已发布 FAQ 库 | 知识沉淀管理 | 知识管理员 | 已上线 FAQ 与缓存状态 |
| `/settlement/gaps` | 知识缺口列表 | 知识沉淀管理 | 知识管理员 | 缺口查看、一键建档 |

### 1.2 菜单与按钮级权限规则

- **动态菜单渲染**：登录后依据后端返回的 `permissions`（菜单码 + 操作码）动态生成侧边栏，无权限菜单不展示、无权限路由需重定向 403。
- **按钮级权限**：增/删/改/查/AI访问 五类操作码控制页面内按钮显隐（如无「删除」权限则隐藏删除按钮，非仅前端隐藏，后端仍需拦截）。
- **路由守卫**：
  - 未携带 Token → 重定向 `/login`；
  - 已登录但访问无权限路由 → 展示 403 页；
  - Token 过期（后端返回 401）→ 清除本地态并跳登录页。

---

## 二、页面级功能详情

### 页面 1：登录与个人中心页

**UI组成**
- 登录表单：用户名输入框、密码输入框（密文）、登录按钮、错误提示区。
- 个人中心：用户头像/名称、所属部门、角色标签列表、登出按钮。

**交互逻辑**
- 点击登录 → 前端做非空校验 → 调用 `POST /api/auth/login` → 成功后持久化 `access_token`、`user_info`、`permissions` 并跳转首页/工作台。
- 登录失败：展示后端返回的错误信息（账号或密码错误 / 账号停用）。
- 登出：清除本地 Token 与权限缓存，跳 `/login`。

**状态管理**
- 登录中：按钮 loading、禁止重复提交。
- 错误态：表单顶部/字段级错误提示。
- 持久化：Token 存 localStorage（[待确认]：是否需 HttpOnly Cookie 方案，见后端安全策略）。

**兼容性要求**：详见「三、通用兼容性要求」。

---

### 页面 2：组织架构与权限管理页（用户/角色/部门）

**UI组成**
- 用户管理：用户列表表格（用户名、显示名、部门、角色、状态、操作列）、顶部搜索栏、新增/编辑弹窗表单、重置密码按钮、启停用开关。
- 角色管理：角色列表 + 权限树（树形多选：菜单项 + 增/删/改/查/AI访问 操作节点）。
- 部门管理：树形部门结构（支持展开/折叠）、新增子部门、编辑、设置负责人、成员关联。

**交互逻辑**
- 用户新增/编辑：弹窗填写 → 前端校验（用户名必填唯一、显示名必填、部门/角色选择）→ 提交 `POST /api/org/users` / `PUT /api/org/users/{id}`。
- 重置密码：二次确认弹窗 → 调用重置接口。
- 启停用：开关切换即时提交，状态列实时刷新。
- 角色权限树：勾选保存 → `POST /api/org/roles/{id}/permissions`。
- 部门树：点击节点展开、右键/按钮新增子部门、拖拽排序（[待确认]：是否要求拖拽排序，若不要求则提供上移/下移按钮）。

**状态管理**
- 列表加载：骨架屏/loading 占位。
- 分页状态：当前页、每页条数、总数。
- 删除操作：乐观更新 + 失败回滚提示。

**兼容性要求**：树形组件需兼容 1024px 以上屏幕。

---

### 页面 3：知识维护与导入页

**UI组成**
- 知识导入中心：文件拖拽区（支持 PDF/Markdown/Word/TXT）、多文件选择按钮、目录批量选择、上传任务列表（文件名、解析状态进度条、结果）。
- 知识单元列表：搜索框（标题/分类/状态）、筛选器、表格（单元编号、标题、分类、格式、数据权限摘要、创建人、更新时间、状态、操作）。
- 编辑/详情页：标题输入、正文富文本/Markdown 编辑器、标签选择、附件上传、数据权限配置组件。

**交互逻辑**
- 上传：拖拽或点击选择 → 前端校验文件格式与大小 → 并发上传 → 轮询解析进度（`GET` 导入进度接口）→ 完成后刷新列表。
- 列表操作：查看跳转详情；编辑跳转编辑页；删除批量勾选后调用 `DELETE /api/knowledge/units`。
- 数据权限配置：统一权限弹窗（勾选全局公开 / 多选部门 / 多选角色 / 多选人员）→ 提交 `POST /api/knowledge/units/{id}/permissions`。

**状态管理**
- 上传进度：任务级进度轮询（[待确认]：轮询间隔建议 2s，超时上限 30s 提示重试）。
- 解析失败：失败态展示错误原因，支持重试。
- 列表缓存：短时缓存 + 操作后主动失效刷新。

**兼容性要求**：拖拽上传需兼容 Chrome/Edge；大文件分片上传（>50MB，[待确认]）。

---

### 页面 4：AI 对话鉴权工作台

**UI组成**
- 提问输入框 + 发送按钮、历史对话列表（会话切换）、流式回复区（Markdown 渲染）、知识引用来源卡片、权限缺失提示卡片。

**交互逻辑**
- 发送提问 → 携带 `question` 与 `session_id` 调用 `POST /api/ai/chat/stream`（SSE）。
- 流式渲染：逐 chunk 追加渲染 Markdown，支持打字机效果。
- 引用卡片：展示命中知识单元标题与链接，点击跳详情（若无权限则禁用跳转）。
- 权限缺失提示：收到 `unauthorized` 片段时渲染醒目提示卡片（黄色警告样式），明确告知缺失访问权限。
- 会话管理：新建会话、切换历史会话、删除会话。

**状态管理**
- 加载态：回复区骨架屏/闪烁光标。
- 流式中断：网络断开显示「连接中断，点击重试」，支持断点续传（[待确认]）。
- 错误态：后端异常时展示友好错误，非原始堆栈。

**兼容性要求**：SSE 需处理 EventSource 兼容与长连接超时。

---

### 页面 5：数据看板页

**UI组成**
- 指标卡片：Agent 访问次数、独立访问人数（UV）、知识单元总数、总 Token 消耗、平均响应时间。
- 排行榜：常见高频问题 TOP 榜、最常访问知识单元 TOP 榜。
- 趋势与分布图：按日/周访问趋势、Token 消耗走势、响应时间分布。

**交互逻辑**
- 进入页面加载 `GET /api/dashboard/metrics` + 各排行榜与趋势接口。
- 时间范围切换（日/周）触发趋势图数据重新拉取。
- 排行榜支持点击问题跳转知识单元详情。

**状态管理**
- 首屏加载：图表区域骨架屏。
- 图表空态：无数据时展示空态插画与文案。
- 数据缓存：指标数据短时缓存（如 60s），切 tab 不重复请求。

**兼容性要求**：图表库（ECharts/Recharts）响应式，适配 1920/1366 宽屏。

---

### 页面 6：知识沉淀管理页（FAQ 推荐审核 / 已发布 FAQ / 知识缺口）

**UI组成**
- FAQ 推荐列表：问题、推荐频次、关联知识单元、建议答案、审核按钮（通过/驳回）。
- 已发布 FAQ 库：FAQ 问答列表、缓存生效状态标签。
- 知识缺口列表：未命中问题、提问频次、最近提问时间、一键创建知识单元按钮。

**交互逻辑**
- 审核：通过 → 弹窗可编辑标准答案 → 提交 `POST /api/settlement/faqs/{id}/review`（`action=approve` + `edited_answer`）；驳回 → `action=reject`。
- 一键建档：从缺口点击创建 → 跳转/弹窗创建知识单元并回填 `resolved_unit_id`。

**状态管理**
- 审核操作：按钮 loading、成功后从推荐列表移除。
- 列表分页与空态。

**兼容性要求**：标准答案编辑支持 Markdown。

---

## 三、通用兼容性要求

| 维度 | 要求 |
| --- | --- |
| 浏览器 | 现代浏览器 Chrome ≥ 90、Edge ≥ 90、Firefox ≥ 88；不支持 IE |
| 屏幕适配 | 桌面端为主，最小支持 1280px 宽；[待确认] 是否需移动端适配 |
| 技术栈 | [待确认] Vue3 / React（推荐 React + TypeScript + Ant Design）|
| 状态管理 | [待确认] Pinia / Redux Toolkit |
| 构建 | Vite/Webpack 打包，生产环境启用 gzip、按路由懒加载 |
| 加载失败重试 | 所有接口统一封装：网络错误/超时自动重试 1 次，仍失败展示重试按钮 |
| 骨架屏 | 所有列表/图表首屏使用骨架屏 |
| 请求层 | 统一 Axios/Fetch 拦截器：注入 Token、401 统一跳登录、错误码统一 toast |

---

## 四、数据校验规则（前端侧）

> 后端为最终校验权威，前端做即时校验提升体验，二者规则保持一致。

| 表单字段 | 校验规则 |
| --- | --- |
| 用户名 | 必填，3-32 字符，字母数字下划线，唯一性由后端校验 |
| 密码 | 登录必填；重置密码 8-32 位含大小写字母+数字 [待确认] |
| 显示名 | 必填，1-50 字符 |
| 角色名/编码 | 必填；编码字母数字下划线，唯一 |
| 知识标题 | 必填，1-200 字符 |
| 知识正文 | 必填，非空 |
| 上传文件 | 格式限 PDF/MD/DOCX/TXT；单文件 ≤ 50MB [待确认] |
| 数据权限 | 至少选择一类实体；`global` 与其他实体互斥（选了全局则无需其他）|
| 审核答案 | 审核通过时答案必填 |

---

## 五、回显逻辑

- 用户编辑：回显用户名（只读）、显示名、部门、角色、状态。
- 知识单元编辑：回显标题、正文、标签、附件、已配置数据权限（勾选回显到权限弹窗）。
- FAQ 审核：回显建议答案到编辑器，可修改后提交。
- 知识缺口建档：回显缺口问题作为新建知识单元的标题/正文预填。

---

## 六、[待确认] 事项汇总（前端）

1. Token 存储方案：localStorage vs HttpOnly Cookie（涉及安全，建议 Cookie）。
2. 大文件（>50MB）是否需分片上传。
3. 部门树是否需拖拽排序。
4. 移动端是否需适配（建议暂不支持）。
5. 前端技术栈选型（建议 React + TS + AntD）。
6. 流式回复是否需断点续传。
7. 重置密码强度规则。
